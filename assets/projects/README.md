# assets/projects

One clean screenshot per project — turns text claims into proof.
Name `<project>-screenshot.png`, ~1200px wide, PNG.

Wanted: `thezao`, `wavewarz` (a live battle), `empire-builder`, `zlank` (deploying a Snap),
`hermes` (an agent run), `zaostock`, `zounz`.
